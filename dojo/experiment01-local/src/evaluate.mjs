// U3 — implement evaluate(node) per spec.md. Import CalcError from ./errors.mjs.
// Returns a number. Throws CalcError{code:'DIVZERO'} on division by zero.
import { CalcError } from "./errors.mjs";

export function evaluate(node) {
  throw new CalcError("PARSE", "not implemented");
}
